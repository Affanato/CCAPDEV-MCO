<h2>Meet the Team</h2>

{{#each teamMembers}}
    <section class="card">
        <div class="photo"></div>
        <div class="name">{{name}}</div>
        <div class="bio">{{bio}}</div>
    </section>
{{/each}}
